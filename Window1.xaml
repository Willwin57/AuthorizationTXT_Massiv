<Window x:Class="WpfApp5.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp5"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <StackPanel Width="200" VerticalAlignment="Center">

            <TextBlock Text="Авторизация" FontSize="22" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Логин:"/>
            <TextBox x:Name="LoginBox" Height="25" Margin="0,0,0,10"/>

            <TextBlock Text="Пароль:"/>
            <PasswordBox x:Name="PassBox" Height="25" Margin="0,0,0,20"/>

            <Button Content="Войти" Height="35" FontSize="14" Click="Button_Enter_Click"/>

        </StackPanel>
    </Grid>
</Window>
